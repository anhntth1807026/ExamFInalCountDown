@model IEnumerable<ExamFinalCountDown.Models.ExamSubject>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ExamSubjectName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ExamSubjectName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ExamSubjectId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ExamSubjectId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ExamSubjectId })
        </td>
    </tr>
}

</table>
